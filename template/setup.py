"""
    This module contains setup instructions for $project_name project
    This simple script is needed to use
        - pip install -e .
        - add $project_name = {editable = true, path = "."} to packages/dev-packages
"""
from setuptools import setup


if __name__ == "__main__":
    setup()
